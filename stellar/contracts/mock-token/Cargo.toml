[package]
name = "mock-token"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "mock_token"

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
